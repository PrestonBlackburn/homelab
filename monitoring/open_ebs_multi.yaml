kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: replicated-monitor-volume-claim
spec:
  storageClassName: openebs-jiva-csi-default
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi